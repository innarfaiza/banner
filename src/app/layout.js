export const metadata = {
  title: "Innar Faiza Syahrani",
  description: "Banner",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
